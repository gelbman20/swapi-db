import React from "react";

import './spinner.css';

const Spinner = () => {
  return (
    <div className="spinner">
      <div className="cssload-box-loading">
      </div>
    </div>
  );
};

export default Spinner;